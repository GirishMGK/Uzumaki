# UTF-8
#
# Windows version-resource metadata embedded in Uzumaki.exe (via Uzumaki.spec's
# `version=` argument). Genuine software almost always carries this; its total
# absence is one of several small signals Windows Defender/SmartScreen weighs
# when a fresh, unsigned .exe gets flagged as "unrecognized." This alone won't
# make SmartScreen trust the file (only code-signing + download reputation do
# that), but it removes one avoidable red flag.
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(0, 0, 0, 0),
    prodvers=(0, 0, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0),
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          u"040904B0",
          [
            StringStruct(u"CompanyName", u"Sangir Analytics"),
            StringStruct(u"FileDescription", u"Uzumaki — audit, loan-audit & document-processing tools hub"),
            StringStruct(u"FileVersion", u"0.0.0.0"),
            StringStruct(u"InternalName", u"Uzumaki"),
            StringStruct(u"LegalCopyright", u"© Sangir Analytics"),
            StringStruct(u"OriginalFilename", u"Uzumaki.exe"),
            StringStruct(u"ProductName", u"Uzumaki"),
            StringStruct(u"ProductVersion", u"0.0.0.0"),
          ],
        )
      ]
    ),
    VarFileInfo([VarStruct(u"Translation", [1033, 1200])]),
  ],
)
